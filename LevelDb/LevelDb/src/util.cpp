#include "util.hpp"

namespace util {
    std::string serialize(const std::string& key, const std::string& value) { return ""; }
    void deserialize(const std::string& data, std::string& key, std::string& value) {}
    uint32_t crc32(const std::string& data) { return 0; }
}
